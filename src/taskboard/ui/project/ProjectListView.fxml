<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>
<?import java.net.URL?>

<BorderPane xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="taskboard.ui.project.ProjectListController"
            styleClass="root">
    
    <stylesheets>
        <URL value="@project-styles.css" />
    </stylesheets>

    <top>
        <VBox spacing="15" styleClass="content-header">
            <padding>
                <Insets top="25" right="30" bottom="20" left="30"/>
            </padding>

            <HBox alignment="CENTER_LEFT" spacing="15">
                <Label text="Quản Lý Dự Án và Thành Viên" styleClass="page-title"/>
                
                <Region HBox.hgrow="ALWAYS" />
                
                <HBox alignment="CENTER_RIGHT" spacing="10">
                    <TextField fx:id="txtKeyword" promptText="🔍 Tìm kiếm dự án theo tên..." 
                               prefWidth="350" styleClass="search-input"/>
                    <Button text="Tìm kiếm" onAction="#handleSearch" styleClass="btn-search"/>
                    <ComboBox fx:id="cbStatus" promptText="Trạng thái" prefWidth="140" 
                              styleClass="combo-filter"/>
                    <Button text="+ Tạo Dự Án Mới" onAction="#handleNewProject" 
                            styleClass="btn-create"/>
                </HBox>
            </HBox>
        </VBox>
    </top>

    <center>
        <TableView fx:id="tblProjects">
            <padding>
                <Insets top="10" right="30" bottom="30" left="30"/> 
            </padding>
            <columnResizePolicy>
                <TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/>
            </columnResizePolicy>
            
            <columns>
                <TableColumn fx:id="colName" text="Tên Dự Án" prefWidth="250"/>
                <TableColumn fx:id="colPM" text="Quản Lý (PM)" prefWidth="180"/>
                <TableColumn fx:id="colStatus" text="Trạng Thái" prefWidth="130" maxWidth="150"/>
                <TableColumn fx:id="colStart" text="Ngày Bắt Đầu" prefWidth="140" maxWidth="160"/>
                <TableColumn fx:id="colEnd" text="Ngày Kết Thúc" prefWidth="140" maxWidth="160"/>
                <TableColumn fx:id="colAction" text="Hành động" sortable="false" prefWidth="150" maxWidth="200"/>
            </columns>
        </TableView>
    </center>
</BorderPane>
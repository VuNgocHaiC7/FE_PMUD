<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.image.Image?>
<?import java.net.URL?>

<BorderPane xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="taskboard.ui.admin.UserManagementController"
            styleClass="root">
    
    <stylesheets>
        <URL value="@admin-styles.css" />
    </stylesheets>

    <top>
        <VBox spacing="15" styleClass="content-header">
            <padding>
                <Insets top="25" right="30" bottom="20" left="30"/>
            </padding>

            <HBox alignment="CENTER_LEFT" spacing="15">
                <Label text="Quản Lý Người Dùng" styleClass="page-title"/>
                
                <Region HBox.hgrow="ALWAYS" />
                
                <HBox alignment="CENTER_RIGHT" spacing="10">
                    <TextField fx:id="txtSearch" promptText="🔍 Tìm kiếm người dùng theo tên hoặc username..." 
                               prefWidth="350" styleClass="search-input"/>
                    <Button text="Tìm kiếm" onAction="#handleSearch" styleClass="btn-search"/>
                    <Button text="+ Tạo Người Dùng" fx:id="btnCreateUser" 
                            styleClass="btn-create"
                            onAction="#handleCreateUser"/>
                </HBox>
            </HBox>
        </VBox>
    </top>

    <center>
        <TableView fx:id="tableUsers">
            <padding>
                <Insets top="10" right="30" bottom="30" left="30"/> 
            </padding>
            <columnResizePolicy>
                <TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/>
            </columnResizePolicy>
            
            <columns>
                <TableColumn fx:id="colId" text="ID" prefWidth="50" maxWidth="70"/>
                <TableColumn fx:id="colUsername" text="Tên đăng nhập"/>
                <TableColumn fx:id="colFullName" text="Họ và tên"/>
                <TableColumn fx:id="colEmail" text="Email"/>
                <TableColumn fx:id="colRole" text="Vai trò" visible="false"/> 
                <TableColumn fx:id="colStatus" text="Trạng thái" prefWidth="120" maxWidth="150"/>
                <TableColumn fx:id="colAction" text="Hành động" sortable="false" prefWidth="150" maxWidth="200"/>
            </columns>
        </TableView>
    </center>
</BorderPane>
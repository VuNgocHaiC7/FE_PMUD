<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import java.net.URL?>

<BorderPane xmlns="http://javafx.com/javafx" xmlns:fx="http://javafx.com/fxml" 
            fx:controller="taskboard.ui.kanbanBoard.KanbanProjectListController"
            styleClass="kanban-root">

    <stylesheets>
        <URL value="@kanban-styles.css"/>
    </stylesheets>

    <!-- Header Section -->
    <top>
        <VBox styleClass="kanban-header">
            <padding>
                <Insets top="30" right="30" bottom="25" left="30"/>
            </padding>
            
            <!-- Title Row -->
            <HBox alignment="CENTER_LEFT" spacing="15">
                <Label text="📋 Bảng Công Việc" styleClass="kanban-page-title"/>
                <Region HBox.hgrow="ALWAYS"/>
                <Label fx:id="lblProjectCount" text="0 dự án" styleClass="project-count-label"/>
            </HBox>
        </VBox>
    </top>

    <!-- Main Content: Grid of Project Cards -->
    <center>
        <ScrollPane fitToWidth="true" fitToHeight="true" styleClass="cards-scroll-pane">
            <VBox styleClass="cards-container" spacing="20">
                <padding>
                    <Insets top="10" right="30" bottom="30" left="30"/>
                </padding>
    
                <FlowPane fx:id="projectCardsPane" 
                          hgap="20" vgap="20" 
                          styleClass="project-cards-grid">
                </FlowPane>
                
                <!-- Empty State -->
                <VBox fx:id="emptyState" alignment="CENTER" spacing="15" 
                      style="-fx-min-height: 400px;" visible="false" managed="false">
                    <Label text="📂" style="-fx-font-size: 48px;"/>
                    <Label text="Không có dự án nào" 
                           style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: #64748b;"/>
                    <Label text="Hãy tạo dự án mới hoặc thay đổi bộ lọc" 
                           style="-fx-font-size: 13px; -fx-text-fill: #94a3b8;"/>
                </VBox>
            </VBox>
        </ScrollPane>
    </center>
    
    <!-- Keep the hidden table for compatibility -->
    <bottom>
        <TableView fx:id="tblProjects" visible="false" managed="false">
            <columns>
                <TableColumn fx:id="colName" text="Tên Dự Án"/>
                <TableColumn fx:id="colPM" text="Quản Lý (PM)"/>
                <TableColumn fx:id="colStatus" text="Trạng Thái"/>
                <TableColumn fx:id="colStart" text="Ngày Bắt Đầu"/>
                <TableColumn fx:id="colEnd" text="Ngày Kết Thúc"/>
            </columns>
        </TableView>
    </bottom>
</BorderPane>

<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.image.Image?>
<?import java.net.URL?>

<BorderPane xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="taskboard.ui.admin.UserManagementController"
            styleClass="root">
    
    <stylesheets>
        <URL value="@admin-styles.css" />
    </stylesheets>

    <top>
        <VBox styleClass="content-header">
            <padding>
                <Insets top="30" right="30" bottom="25" left="30"/>
            </padding>
            
            <!-- Title Row -->
            <HBox alignment="CENTER_LEFT" spacing="15">
                <Label text="👥 Quản Lý Người Dùng" styleClass="page-title"/>
            </HBox>
            
            <!-- Search & Filter Row -->
            <HBox spacing="12" alignment="CENTER_LEFT" style="-fx-padding: 15 0 0 0;">
                <ComboBox fx:id="cbRoleFilter" prefWidth="160" prefHeight="38"
                          promptText="Tất cả"
                          styleClass="filter-combo"/>
                <TextField fx:id="txtSearch" prefWidth="350" prefHeight="38"
                           promptText="🔍 Tìm kiếm người dùng theo tên hoặc username..."
                           styleClass="search-field"/>
                <Button text="Tìm kiếm" onAction="#handleSearch" prefHeight="38"
                        styleClass="search-btn"/>
                <Region HBox.hgrow="ALWAYS"/>
            </HBox>
        </VBox>
    </top>

    <center>
        <StackPane>
            <!-- Thông báo cho Member -->
            <VBox fx:id="vboxMemberMessage" alignment="CENTER" spacing="20" 
                  style="-fx-background-color: #f8f9fa; -fx-padding: 50;">
                <Label text="Member không được phép truy cập vào danh sách người dùng" 
                       style="-fx-font-size: 18px; -fx-text-fill: #dc3545; -fx-font-weight: bold;"/>
                <Label text="Chỉ có Admin mới có quyền xem và quản lý người dùng." 
                       style="-fx-font-size: 14px; -fx-text-fill: #6c757d;"/>
            </VBox>
            
            <!-- Bảng người dùng cho Admin -->
            <TableView fx:id="tableUsers">
                <padding>
                    <Insets top="10" right="30" bottom="30" left="30"/> 
                </padding>
                <columnResizePolicy>
                    <TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/>
                </columnResizePolicy>
                
                <columns>
                    <TableColumn fx:id="colId" text="ID" prefWidth="50" maxWidth="70"/>
                    <TableColumn fx:id="colUsername" text="Tên đăng nhập"/>
                    <TableColumn fx:id="colFullName" text="Họ và tên"/>
                    <TableColumn fx:id="colEmail" text="Email"/>
                    <TableColumn fx:id="colRole" text="Vai trò" visible="false"/> 
                    <TableColumn fx:id="colStatus" text="Trạng thái" prefWidth="120" maxWidth="150"/>
                    <TableColumn fx:id="colAction" text="Hành động" sortable="false" prefWidth="280" minWidth="280" maxWidth="300"/>
                </columns>
            </TableView>
        </StackPane>
    </center>
</BorderPane>